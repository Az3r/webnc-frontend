import { makeStyles } from '@material-ui/core'

export default makeStyles((theme) => ({
  form_duration: {
    padding: theme.spacing(1, 0),
    alignItems: 'center'
  },
  input_duration: {
    width: 48
  }
}))
